{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Modern portfolio with React + Vite + Node.js",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client",
    "start": "node server/server.js"
  }
}